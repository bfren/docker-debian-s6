name: publish-alpine3_15

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish-alpine3_15:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout code
        uses: actions/checkout@v3
      -
        name: Get repository name
        run: echo "REPOSITORY_NAME=$(echo '${{ github.repository }}' | awk -F '/' '{print $2}')" >> $GITHUB_ENV
        shell: bash
      -
        name: Read Alpine version - revision
        uses: bfren/read-file@v1
        with:
          file: ./3.15/ALPINE_REVISION
        id: alpine_revision
      -
        name: Read image version
        uses: bfren/read-file@v1
        with:
          file: ./VERSION_MAJOR
        id: version_major
      -
        name: Read image version
        uses: bfren/read-file@v1
        with:
          file: ./VERSION_MINOR
        id: version_minor
      -
        name: Read image version
        uses: bfren/read-file@v1
        with:
          file: ./VERSION
        id: version
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      -
        name: Build and push
        id: docker_build
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./3.15/Dockerfile
          build-args: |
            BF_IMAGE=${{ env.REPOSITORY_NAME }}
            BF_VERSION=${{ steps.version.outputs.contents }}
          push: true
          platforms: linux/amd64,linux/arm/v7,linux/arm64
          tags: |
            bfren/alpine-s6:alpine3.15
            bfren/alpine-s6:alpine3.15-${{ steps.version_major.outputs.contents }}
            bfren/alpine-s6:alpine3.15-${{ steps.version_minor.outputs.contents }}
            bfren/alpine-s6:alpine3.15-${{ steps.version.outputs.contents }}
            bfren/alpine-s6:alpine${{ steps.alpine_revision.outputs.contents }}
            ghcr.io/bfren/alpine-s6:alpine3.15
            ghcr.io/bfren/alpine-s6:alpine3.15-${{ steps.version_major.outputs.contents }}
            ghcr.io/bfren/alpine-s6:alpine3.15-${{ steps.version_minor.outputs.contents }}
            ghcr.io/bfren/alpine-s6:alpine3.15-${{ steps.version.outputs.contents }}
            ghcr.io/bfren/alpine-s6:alpine${{ steps.alpine_revision.outputs.contents }}
      -
        name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
