#!/bin/sh

set -euo pipefail
export BF_E=`basename ${0}`


#======================================================================================================================
# Placeholder so permissions can be set in base image.
#======================================================================================================================

bf-debug "Base healthcheck always returns healthy."
exit 0
